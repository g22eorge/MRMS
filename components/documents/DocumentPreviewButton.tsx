/**
 * Documents alias for the shared {@link RecordPreviewButton}. Kept so existing
 * `@/components/documents/DocumentPreviewButton` imports resolve unchanged; new
 * code outside the documents module should import from `@/components/record`.
 */
export { RecordPreviewButton as DocumentPreviewButton } from "@/components/record/RecordPreviewButton";
