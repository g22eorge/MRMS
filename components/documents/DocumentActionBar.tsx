/**
 * Documents alias for the shared {@link RecordActionBar}. Kept so existing
 * `@/components/documents/DocumentActionBar` imports resolve unchanged; new code
 * outside the documents module should import from `@/components/record`.
 */
export { RecordActionBar as DocumentActionBar } from "@/components/record/RecordActionBar";
